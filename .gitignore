# Large build inputs and derived data: reproducible from build_graph_from_reference.py
cache/
# Dependencies and build output
node_modules/
dist/
.venv/
__pycache__/
*.pyc
.pytest_cache/
# Local runtime state, checkpoints and logs
brain/runs/**/_smoke.json
# Run artifacts of the four-arm experiment and the demo's own trained brain. \`brain/runs\` is
# mixed on purpose: curriculum/ and promptindex/ hold the SHIPPED checkpoints the API loads and
# ARE tracked, so this names the experiment directory rather than ignoring brain/runs wholesale.
# of checkpoints into a published tree. of checkpoints into a published tree.
brain/runs/plastic_brain/
artifacts/
*_real_run.log
